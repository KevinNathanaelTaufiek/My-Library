<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevinnt.mylibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/buku"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLibrary">
        <activity android:name=".WebsiteActivity" android:label="Developer"></activity>
        <activity android:name=".FavouriteActivity" android:label="Favourite"/>
        <activity android:name=".WishlistActivity" android:label="Wishlist"/>
        <activity android:name=".CurrentlyReadActivity" android:label="Currently Read"/>
        <activity android:name=".AlreadyReadActivity" android:label="Already Read"/>
        <activity android:name=".BookActivity" android:label="Book"/>
        <activity android:name=".AllBooksActivity" android:label="All Books"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>